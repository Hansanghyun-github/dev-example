env:
  data: "test"

spring:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    #open-in-view: false
  output:
    ansi:
      enabled: always

logging.level:
  org.hibernate.SQL: debug
  com:
    example:
      devexample:
        dev: trace

mybatis:
  type-aliases-package: com.example.devexample.dev,com.example.devexample.required.domain,com.example.devexample.statistics.view
  configuration:
    map-underscore-to-camel-case: true